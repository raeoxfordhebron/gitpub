<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <style type="text/css">
        body {
            background-color: grey;
            color: white;
        }
    </style>
    <h1>
        Welcome to gitPub
    </h1>
    <ul>
        <% for(let i = 0; i < drinks.length; i++) { %>
            <% let splitDrinks = drinks[i].name.split("") %>
            <% let capitalDrinks = splitDrinks[0].toUpperCase() %>
            <% splitDrinks.splice(0, 1, capitalDrinks) %>
            <% let joinDrinks = splitDrinks.join("") %>
            <a href="/drinks/<%= i %>"><li>
                <%= joinDrinks %>
                
            </li></a>
       <% } %>
    </ul>
</body>
</html>